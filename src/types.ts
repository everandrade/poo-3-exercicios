export interface StudentDB {
    id: string,
    name: string,
    email: string,
    classroom: string,
    created_at: string
}

export interface StudentDBPost {
    id: string,
    name: string,
    email: string,
    classroom: string
}